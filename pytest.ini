[pytest]
addopts = -ra --strict-markers
markers =
    unit: pure helper and rule tests
    contract: schema and response contract tests
    integration: route, database, and filesystem integration tests
    smoke: critical happy-path verification tests
    system: multi-step subsystem tests
